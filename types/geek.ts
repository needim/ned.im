export interface GeekPost {
  title: string;
  description: string;
  date: string;
  videoUrl: string;
  slug: string;
  attachmentUrl?: string;
}

export interface GeekMeta {
  title: string;
  description: string;
  date: string;
  videoUrl: string;
  attachmentUrl?: string;
} 